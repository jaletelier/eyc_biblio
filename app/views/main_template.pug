html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')  
<% for (var css in htmlWebpackPlugin.files.css) {%>    link(href="<%= htmlWebpackPlugin.files.css[css] %>", rel="stylesheet")<% } %>
    block head  
  body
    block body
<% for (var chunk in htmlWebpackPlugin.files.chunks) { %>    script(src="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>")<% } %>
    block scripts